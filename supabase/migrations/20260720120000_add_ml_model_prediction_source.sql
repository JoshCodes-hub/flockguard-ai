-- Migration: Add 'ml_model' to predictions.prediction_source check constraint
-- Generated by assistant to allow ML model predictions to be saved.

BEGIN;

ALTER TABLE public.predictions
  DROP CONSTRAINT IF EXISTS predictions_prediction_source_check;

ALTER TABLE public.predictions
  ADD CONSTRAINT predictions_prediction_source_check
  CHECK (prediction_source IN ('rule_engine','vision_ai','ml_model'));

COMMIT;
